启动时必须在 `index.tsx` 中优先执行 console 日志捕获与调试模块安装，以保证早期错误能被记录到内存缓冲和运行时日志文件；`bootstrap.tsx` 中的 `initModVisualizers()` 必须早于 `restoreStoredModVisualizer` 完成，否则 localStorage 中保存的 `mod:` 前缀可视化器模式会在 settings store 校验阶段回退到 classic。