React + ReactDOM.createRoot 进行客户端渲染；Vite 构建并注入 `__COMMIT_HASH__`、`__APP_VERSION__` 等编译期常量；Zustand 管理设置与 UI 状态；framer-motion 驱动可视化器动画；i18next 处理多语言；Electron 通过 `window.electron` 暴露 IPC 桥接（preload 侧实现）。