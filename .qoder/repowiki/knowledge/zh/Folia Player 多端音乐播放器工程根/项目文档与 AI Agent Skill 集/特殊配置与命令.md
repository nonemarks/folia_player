Skill 中的验证命令依赖仓库根脚本：`npm run test:unit`、`npm run test:ui`、`npm run test:ui:update`、`npm run dev:probe`、`vercel dev`、`npm run dev:electron` 等；酷狗对齐 skill 要求先准备 `.env.local` 中的 `VITE_KUGOU_API_BASE` 和本地凭据文件 `test-results/.dev-credentials`，再通过 `pwsh -NoProfile -File skills/kugou-provider-alignment/scripts/probe-ku-gou.ps1` 发起请求。