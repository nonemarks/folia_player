UI 测试需要 Playwright 浏览器环境并依赖 `package.json` 中的 `__APP_VERSION__` 注入常量；截图快照位于 `test/ui/app.screenshot.spec.ts-snapshots/`，新增或修改界面时需重新生成比对。手工脚本需按各自入口单独执行（如 `node test/manual/netease_lyrics_cli.ts`、`tsx test/manual/stage-client/main.ts`），无统一测试命令。