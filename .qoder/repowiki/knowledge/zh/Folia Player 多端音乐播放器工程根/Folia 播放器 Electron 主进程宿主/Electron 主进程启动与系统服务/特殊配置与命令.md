启动 QQ API 前需设置 `PORT`、`AUTO_OPEN_EXPLORER=false`、`QQ_DISABLE_UPDATE_CHECK=true`，可选 `QQ_AUTH_STATE_PATH`，并在 `finally` 中还原环境变量；Whisper 对齐要求 `whisper-cli`（或 PATH 中可执行）与可选的 ffmpeg，模型默认下载到 userData 下的 `whisper-models` 目录，音频转换产物与转录 JSON 输出位于系统临时目录。