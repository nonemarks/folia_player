Linux 下可通过设置环境变量 `FOLIA_PASSWORD_STORE=auto|gnome-libsecret|kwallet5|...` 覆盖 Chromium 密码后端检测；Windows 壁纸模式需要存在 `folia-wallpaper-helper.exe` 二进制且当前主窗口拥有有效 HWND；Wayland 模式下若 `windowtolayer` 父进程死亡会自动重启为普通窗口并清除 `wallpaper_mode`。