Discord Rich Presence 通过 `@xhayper/discord-rpc` IPC 传输；Linux 密码存储强制 `gnome-libsecret` 后端（跳过 KDE kwallet）；Windows 壁纸模式通过 `child_process.spawn` 启动 Rust 编写的 `folia-wallpaper-helper.exe`，以 JSONL 事件流通信；Wayland/X11 壁纸模式通过外部 `windowtolayer` 包装进程模型。